fragment ProfileSectionFragment on User {
  id
  votedCount
  imageURL
  displayName {
    ja
  }
  firstName
  lastName
  username
  generation
  friendsCount
  schoolId
  grade
  school {
    id
    name
    shortName
  }
}