query PeopleYouMayKnow($first: Int!) {
  fromSchool(first: $first) {
    edges {
      node {
        id
        imageURL
        firstName
        lastName
        displayName {
          ja
        }
        grade
      }
    }
  }
}