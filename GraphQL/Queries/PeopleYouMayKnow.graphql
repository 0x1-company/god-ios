query PeopleYouMayKnow($first: Int!) {
  usersBySameSchool(first: $first) {
    edges {
      node {
        id
        imageURL
        firstName
        lastName
        displayName {
          ja
        }
        grade
      }
    }
  }
}