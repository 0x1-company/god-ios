query Schools($first: Int!, $after: String) {
  schools(first:$first, after:$after) {
    edges {
      node {
        id
        name
        shortName
        usersCount
        profileImageURL
      }
    }
  }
}