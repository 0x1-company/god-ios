# import '../fragments/ProfileSection.graphql'

query CurrentUser {
  currentUser {
    id
    firstName
    lastName
    username
    generation
    gender
    friendsCount
    schoolId
    school {
      id
      name
      shortName
    }
    wallet {
      coinBalance
		}
    ...ProfileSection
  }
}
