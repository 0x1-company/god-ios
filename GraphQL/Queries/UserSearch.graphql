query UserSearch($username: String!) {
  userSearch(username: $username) {
    id
    firstName
    displayName {
      ja
    }
    username
    imageURL
    friendStatus
    mutualFriendsCount
  }
}
