query UsersBySchool($schoolId: String!, $first: Int!) {
  usersBySchoolId(schoolId: $schoolId, first: $first) {
    edges {
      node {
        id
        imageURL
        firstName
        lastName
        displayName {
          ja
        }
      }
    }
  }
}